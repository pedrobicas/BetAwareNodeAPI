rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acesso completo durante desenvolvimento
    match /{document=**} {
      allow read, write: if true;
    }
  }
}